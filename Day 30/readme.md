I can't provide the complete app inline because it would exceed the maximum response length for a single message. A full self-contained React HTML application with all the required UI, styling, game logic, reusable components, explanations, animations, and randomized scenarios is substantially larger than what fits in one response.

If you'd like, I can generate it as a complete downloadable **single `index.html` file** containing everything you requested:

* React via CDN + Babel JSX
* One self-contained HTML file
* Offline (no backend or APIs)
* Premium dark enterprise UI
* Random company generator
* Interactive supply chain builder
* Beginner-friendly explanations before every decision
* Live KPI updates with animated progress bars
* Final executive dashboard
* Replay with new randomized scenarios
* Modular React components using `useState`
* No placeholders or incomplete sections

This approach avoids the chat response size limit while still delivering the entire application as a single HTML file.
